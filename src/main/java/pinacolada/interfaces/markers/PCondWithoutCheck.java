package pinacolada.interfaces.markers;

/* Marks PConds that should always be triggered as a triggered action when checked (i.e. should not trigger semi-limited/limited) */
public interface PCondWithoutCheck
{
}
